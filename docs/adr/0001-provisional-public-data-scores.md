# Prefer transparent provisional coverage over strict score withholding

The framework will produce directional provisional scores from legitimate public observations even when institutional components are unavailable. Every such score must expose its calibration status, data coverage, source scope, missing components, and calculation; missing observations reduce coverage rather than being invented, while a category with no valid observation remains unscored.
